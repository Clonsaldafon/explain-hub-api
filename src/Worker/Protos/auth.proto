syntax = "proto3";

option csharp_namespace = "AuthService";

service AuthGrpcService {
  rpc GetUserEmail (UserRequest) returns (UserEmailResponse);
}

message UserRequest {
  string user_id = 1;
}

message UserEmailResponse {
  string email = 1;
}